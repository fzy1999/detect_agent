rules = """## 指标数据异常检测规则：

================================================================

Step 0 准备输入

• 指标名称、单位、采样周期、所在业务/主机组

• 业务 SLO/SLA、故障分级策略

================================================================

Step 1 语义解析 & 监控目标确认

1.1 拆解名字，弄清“指标的业务含义”“可能的异常模式”。

　　示例：

　　OSLinux-CPU_CPU_CPUCpuUtil → CPU 使用率（0-100%，越高越危险）

　　OSLinux-CPU_CPU_CPUidleutil → CPU Idle（0-100%，越低越危险）



1.2 明确异常场景：

阈值判定：
　　• 上限型：超过阈值才算异常（CPUCpuUtil、FSCapacity、MEMUsedMemPerc）

　　• 下限型：低于阈值才算异常（CPUidleutil）

　　• 双尾型：上下都有风险（极少见，通常是 QPS、延迟类）

波动判定：
    向上波动率：
    向下波动率：
    中断上报：

同环比判定： 
    同比上周均值
    同比昨天
    同比上周同天
    环比前10个点均值
    环比前1个点
    环比上周前一个点
    环比昨天前一个点


其他异常场景：
    - 向上的尖刺：指标数值单点向上突增。
        
    - 向下的尖刺：指标数值单点向下突降。
        
    - 向上的漂移：指标数值连续若干点平台上升。
        
    - 向下的漂移：指标数值连续若干点平台下降。
        
    - 趋势性上涨：指标数值连续若干点持续上升。
        
    - 趋势项下跌：指标数值连续若干点持续下降
================================================================

Step 2 数据摸底 (Profiling)

2.1 拉最近30–45天全量点：min、max、avg、p50/p75/p95、工作日/周末拆分曲线。

2.2 观测是否存在：

　　a) 显著周期（日/周）

　　b) 趋势性（长期变高 or 变低）

　　c) 抖动幅度（σ/均值）

　　d) 大量缺失值或采样间隔异常



================================================================

Step 3 规则能解决的一律先用规则

3.1 静态阈值 – 数据分布 + 行业普遍安全线



3.2 分段阈值 / 组合条件

　　• CPUCpuUtil > 80% 且 trend(15 min) > +5% → 说明正在快速升温

　　• FSCapacity > 75% 且 d/dt(1h) > 2% → 盘在被写爆

3.3 周期化阈值

　　若 Step 2 判定存在明显昼夜节律：

　　• 用 7×24 小时槽的 P95 作为每个槽的上阈，P5 作为下阈。

　　• 以 Grafana/Prometheus recording rule 方式每天更新。

能用到这里就 STOP，发布规则、写回 CMDB。

================================================================

Step 4 规则描述困难 → 引入“轻量级”算法

适用场景：① 上下浮动剧烈 ② 周期不稳定 ③ 业务侧要“自动学习”

4.1 指标无缺失、稳定采样：

　　• EWMA + 3σ (单尾型取上界 / 下界)

　　• STL 分解 + residual 3σ （解决非固定周期）

4.2 指标缺失或离散：

　　• 最新 N = 20 点 IQR 法

　　• 针对离散事件计数用 Poisson CUSUM

参数基线都靠最近 7d 数据滚动更新。

================================================================

Step 5 仍有漏报/误报 → 使用“重算法”

5.1 季节性 + 长趋势：Facebook Prophet / Holt-Winters

5.2 复杂噪音：ARIMA / SARIMA

5.3 多维耦合（CPU + Load + MEM）：IsolationForest、One-Class SVM、深度自编码

　　• 先做 Z-score 归一化

　　• 每日离线更新模型，推理走线上

================================================================

Step 6 告警策略与回调

6.1 告警分级：Warning / Major / Critical

6.2 抑制策略：同主机同维度 30 min 内最多一次；自动关闭需满足“恢复阈值” + “保持 10 min 稳定”。

6.3 回调：Prometheus AlertManager → 企业微信 / PagerDuty → 自动创建工单。

================================================================

Step 7 效果评估 & 调参闭环

• 每两周 Review：召回率、误报率、MTTA、生产事故复盘

• 指标换版 / 桶位变动即重新走 Step 1–Step 3

================================================================

总结（思维导图式）

理解指标 → 数据摸底 → 静态阈值 → 动态阈值 → 简单算法 → 复杂算法 → 告警与复盘

“先规则后算法”能让 70-80% 的资源指标一次解决；只有当业务周期性混杂、趋势漂移明显或多维耦合度高时，才值得投入机器学习。

您不应该做的事情：

不要在回复中包含任何编程语言（Python）。 相反，您应该用自然语言（中文）提供一个有序的步骤列表，并给出具体的描述。
不要自行将时间戳转换为日期时间或将日期时间转换为时间戳。 这些详细过程将由执行者处理。
不要使用本地数据（在特定时间段内过滤/缓存的序列）来计算聚合的“组件-KPI”时间序列的全局阈值。 始终使用指标文件中特定组件的整个KPI序列（通常包括一天的KPI）来计算阈值。为了获得全局阈值，您可以先聚合每个组件的每个KPI以计算其阈值，然后检索目标时间段的聚合“组件-KPI”以进行异常检测和尖峰过滤。
不要通过Python可视化数据或绘制图片或图表。 执行者只能提供基于文本的结果。代码中绝不包含matplotlib或seaborn库。
不要在本地文件系统中保存任何内容。 将中间结果缓存到IPython Kernel中。代码单元中绝不使用bash命令。
不要在给定时间段内过滤数据后再计算阈值。 始终在过滤给定时间段数据之前，使用指标文件中特定组件的整个KPI序列计算全局阈值。
不要在不知道有哪些KPI可用时查询特定KPI。 不同系统可能有完全不同的KPI命名约定。如果您想查询特定KPI，请首先确保您了解所有可用的KPI。
不要错误地将追踪中包含故障组件的下游端健康（非故障）服务识别为根本原因。 根本原因组件应是追踪调用链中出现的最下游故障服务，必须首先是通过指标分析识别出的故障组件。
不要在日志分析时仅关注警告或错误日志。许多信息日志包含有关服务操作和服务之间交互的关键信息，这些信息对根本原因分析非常有价值。
不要在回复中使用英文。 使用中文。
"""