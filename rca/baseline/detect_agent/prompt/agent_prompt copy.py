rules = """## 指标数据异常检测规则：

Step 0：准备工作（Pre-flight Check - 万事开头先搞清家底）

这个步骤就像打仗前要摸清地形和兵力。

指标名称、单位、采样周期、所在业务/主机组：

指标名称 (Metric Name): 比如 cpu_utilization、qps、disk_free_space。这是我们要监控的具体对象。
单位 (Unit): 是百分比(%)？是字节(bytes)？是次数/秒 (count/s)？搞错单位，阈值就全错了！比如磁盘空间，是 MB 还是 GB，差了1000倍呢！
采样周期 (Sampling Period): 多久采集一次数据？15秒？1分钟？5分钟？这会影响我们判断“连续N个点”异常时的N值。
所在业务/主机组 (Service/Host Group): 这个指标是哪个业务的？影响的是核心交易还是边缘日志？是数据库服务器还是Web服务器？这直接关系到后面告警的级别和处理优先级。
业务 SLO/SLA、故障分级策略：

SLO/SLA (Service Level Objective/Agreement): 这是我们对业务的承诺。比如“核心交易接口99.9%可用性”，“平均响应时间低于200ms”。我们的告警规则最终要为这些目标服务。如果CPU 80%了，但SLO还好好的，可能只是警告；如果CPU 60%但SLO已经受影响了，那可能就是严重告警。
故障分级策略 (Incident Severity Levels): 通常分为 P1 (Critical/紧急), P2 (Major/严重), P3 (Minor/一般), P4 (Warning/警告)。不同的指标异常，根据对业务的影响程度，会对应到不同的故障级别。比如核心数据库CPU满了是P1，某个非关键日志服务的磁盘空间70%了可能是P3。
老鸟心得： 这一步最容易被忽略，但它决定了你后续所有规则的“意义”。没有业务背景的告警都是瞎忙活。

Step 1：理解指标，明确目标（Know Your Metric & What to Catch）

这一步是搞清楚我们要监控的这个“东西”到底是啥，它怎么动才算“不正常”。

1.1 拆解名字，搞懂含义和“脾气”
*   指标的业务含义：
*   OSLinux-CPU_CPU_CPUCpuUtil: Linux操作系统的CPU整体使用率。
*   OSLinux-CPU_CPU_CPUidleutil: Linux操作系统的CPU空闲率。
*   myapp_orders_qps: 我的应用（myapp）的订单模块每秒请求数。
*   database_slow_queries_count: 数据库慢查询的数量。
*   可能的异常模式（“脾气”）：
*   CPUCpuUtil：0-100%，越高越代表CPU忙，持续高就危险。
*   CPUidleutil：0-100%，越低越代表CPU忙（因为空闲少了），持续低就危险。
*   myapp_orders_qps：可能是0到几万，突然变得很高（可能是攻击或异常流量），或者突然变得很低甚至为0（可能是服务挂了或依赖出问题），都危险。

1.2 明确要抓哪几种“异常场景”
这里手册列得很全，我给你翻译翻译，加点例子：

markdown
复制代码
*   **阈值判定（画条线，过了就算）：**
    *   **上限型：** 不能太高！
        *   `CPUCpuUtil > 90%` (CPU使用率超过90%)
        *   `MEMUsedMemPerc > 85%` (内存使用百分比超过85%)
        *   `FSCapacity > 80%` (磁盘使用量超过80%)
    *   **下限型：** 不能太低！
        *   `CPUidleutil < 10%` (CPU空闲率低于10%)
        *   `myapp_cache_hit_rate < 80%` (我的应用缓存命中率低于80%)
        *   `available_database_connections < 5` (可用数据库连接数少于5个)
    *   **双尾型：** 不能太高，也不能太低！
        *   `myapp_orders_qps`: 比如正常波动是 1000-2000 QPS。如果突然变成 5000 QPS (太高，可能被刷)，或者突然变成 100 QPS (太低，可能服务出问题)，都要告警。
        *   P99延迟：比如正常是 200ms，如果变成 2000ms (太高，用户体验差)，或者突然变成 1ms (太低，可能数据没查全或者逻辑错了)，也可能是问题。

*   **波动判定（变化太快，不对劲）：**
    *   **向上波动率：** 短时间内涨得太猛！
        *   `cpu_utilization` 在5分钟内，平均值从30%涨到了70% (+40%绝对值，或者说涨了133%相对值)。
        *   `increase(nginx_requests_total[5m]) / nginx_requests_total[5m] > 0.5` (5分钟内请求数增长超过50%)
    *   **向下波动率：** 短时间内跌得太惨！
        *   `qps` 在1分钟内，从1000跌到了100 (-90%)。
        *   `decrease(online_users[5m]) / online_users[5m] > 0.3` (5分钟内在线用户数下降超过30%)
    *   **中断上报 (No Data / Heartbeat Loss):** 指标突然不报了！这往往是大问题，比如机器挂了，采集Agent崩了。
        *   `absent(my_custom_metric[5m])` (我的自定义指标5分钟没上报数据了)

*   **同环比判定（跟过去比，不正常）：**
    *   **同比上周均值/同天：** 这个值跟上周一的这个时候比，是不是差太多了？
        *   `qps < (avg_over_time(qps[1h] offset 1w) * 0.7)` (当前1小时QPS均值，比上周同一小时均值的70%还低)
    *   **同比昨天：** 跟昨天这个时候比。
        *   `error_rate > (avg_over_time(error_rate[10m] offset 1d) * 2)` (当前10分钟错误率均值，是昨天同一时刻均值的2倍还多)
    *   **环比前N个点均值/前1个点：** 跟刚才比，是不是突然变了？
        *   `latency_p99 > avg_over_time(latency_p99[10m]) * 1.5` (当前P99延迟，比过去10分钟的平均P99延迟高了50%)

*   **其他异常场景（看形状，不对劲）：**
    *   **向上的尖刺 (Spike Up):** 突然一个点非常高，然后又下来了。可能是瞬时压力，也可能是采集错误。
    *   **向下的尖刺 (Spike Down):** 突然一个点非常低。
    *   **向上的漂移 (Drift Up):** 慢慢地，整体水平抬高了。比如内存泄漏，内存使用率会一点点往上涨。
    *   **向下的漂移 (Drift Down):** 慢慢地，整体水平降低了。
    *   **趋势性上涨/下跌 (Trend Up/Down):** 持续往一个方向变化。比如磁盘空间被一个大日志持续写入，可用空间就会趋势性下跌。
老鸟心得： 不要一上来就想搞个“万能规则”。先从最简单、最明确的“阈值”和“中断上报”开始。同环比和波动判定是进阶玩法，当你发现简单阈值不灵光的时候再考虑。

Step 2：数据摸底（Profiling - 给指标“画个像”）

这一步是拉出指标过去一段时间（比如30-45天）的数据，画出它的曲线图，看看它长什么样，有什么规律。

2.1 拉历史数据，看统计值和曲线
*   全量点： 把过去30-45天的数据都拉出来。
*   统计值： min（最小值）, max（最大值）, avg（平均值）, p50（中位数，一半数据比它小）, p75（75%的数据比它小）, p95（95%的数据比它小，常用来定阈值参考）。
*   工作日/周末拆分曲线： 很多业务指标在工作日和周末表现不一样，比如办公OA系统，周末QPS肯定低。分开看能发现不同模式。

2.2 观察数据“画像”
*   a) 显著周期（日/周）：
*   是不是每天早上9点开始高，下午6点开始低？（日周期，比如办公系统QPS）
*   是不是周一到周五高，周末低？（周周期，比如企业内部服务）
*   为什么重要？ 有周期的指标，用固定阈值很容易误报。比如电商业务，半夜QPS很低是正常的，白天很高也是正常的。

markdown
复制代码
*   **b) 趋势性（长期变高 or 变低）：**
    *   是不是整体在慢慢往上涨（比如内存泄漏导致的内存使用率）？或者慢慢往下跌（比如磁盘可用空间）？
    *   **为什么重要？** 有趋势的指标，固定阈值迟早会失效。

*   **c) 抖动幅度（σ/均值，即变异系数 Coefficient of Variation）：**
    *   这个指标平时是不是就上蹿下跳很厉害？还是比较平稳？
    *   **为什么重要？** 抖动厉害的指标，固定阈值很难定，稍微定低一点就疯狂告警。

*   **d) 大量缺失值或采样间隔异常：**
    *   数据是不是缺一块少一块的？或者有时候1分钟采一个点，有时候5分钟才采一个点？
    *   **为什么重要？** 数据质量差，后面很多算法和规则都没法用，或者效果很差。首先要解决数据采集问题。
老鸟心得： 这一步非常非常重要！看图说话是运维的基本功。 Grafana 这类工具是你最好的朋友。多花点时间在看图上，你会对指标的“脾气”有更深的理解，后面定规则就胸有成竹了。很多时候，一个“拍脑袋”的阈值，就是因为没好好做这一步。

Step 3：规则能解决的一律先用规则（Simple Rules First! - KISS原则）

KISS = Keep It Simple, Stupid! 这是黄金法则。能用简单规则搞定的，绝对不要上复杂算法。简单规则易于理解、易于配置、易于维护。

3.1 静态阈值 (Static Threshold) – 数据分布 + 行业普遍安全线
*   怎么定？
*   参考 Step 2 的P95/P99值：比如过去30天，CPU使用率的P95是70%，那你可以把警告阈值设在75%-80%，严重阈值设在85%-90%。
*   行业普遍安全线：比如CPU使用率，通常认为持续超过80%要注意，超过90%就比较危险了。磁盘空间，超过75%警告，超过85%严重。这些是经验值。
*   结合业务SLO：如果业务对延迟非常敏感，那网络延迟的阈值可能就要定得非常苛刻。
*   示例：
*   CPUCpuUtil > 85% (持续5分钟) -> Critical告警
*   FSCapacity > 75% (持续10分钟) -> Warning告警

3.2 分段阈值 / 组合条件 (Conditional / Tiered Thresholds)
*   当单一阈值不够精确时，可以用组合条件。
*   示例：
*   CPUCpuUtil > 80% 并且 rate(cpu_steal_time[5m]) > 0.1 (CPU使用率高，并且CPU被偷的时间也多，这在虚拟机环境很关键) -> 可能是宿主机有问题。
*   手册里的例子很好：
*   CPUCpuUtil > 80% 且 trend(CPUCpuUtil[15m]) > 5 (CPU使用率大于80%，并且过去15分钟的增长趋势超过5个百分点，说明在快速恶化)
*   FSCapacity > 75% 且 deriv(FSCapacity[1h]) > 0.02 (磁盘使用率大于75%，并且过去1小时的增长速率超过2%/小时，说明磁盘正在被快速写满)
*   http_requests_total{status="5xx"} / http_requests_total > 0.05 且 http_requests_total > 100 (5xx错误率超过5%，并且总请求数大于100，避免在请求量极少时误报)

3.3 周期化阈值 (Periodic Thresholds)
*   如果 Step 2 发现指标有明显的日周期或周周期（比如电商的QPS，白天和晚上不一样）。
*   怎么做？
*   把一天24小时，或者一周7x24小时，切分成很多小时间段（比如每小时一个槽）。
*   计算每个时间段过去几周的P95值作为这个时间段的上限阈值，P5值作为下限阈值。
*   用监控系统（如Prometheus的Recording Rule）每天自动更新这些动态阈值。
*   示例：
*   对于QPS指标，周一上午9-10点的阈值可能是 (800, 1200)，而凌晨3-4点的阈值可能是 (50, 150)。
*   老鸟提示： 这个稍微高级一点，但对于周期性明显的业务指标非常有效，能大大减少误报。

“能用到这里就 STOP，发布规则、写回 CMDB。”
这句话是精髓！ 如果上面这些简单规则已经能覆盖大部分异常场景，并且误报不多，那就先用起来！把规则配置、告警联系人、处理预案等信息记录到CMDB（配置管理数据库）或Wiki里。不要过度设计！

Step 4：规则描述困难 → 引入“轻量级”算法（Smarter Rules - When Simple Isn't Enough）

当指标的“脾气”比较古怪，比如：

① 上下浮动剧烈，没有固定范围。
② 周期不稳定，不是简单的日周期周周期。
③ 业务侧希望系统能“自动学习”正常模式，而不是人工频繁调阈值。
这时候，可以考虑一些简单的统计学方法。

4.1 指标无缺失、稳定采样（数据质量好）：
*   EWMA (Exponentially Weighted Moving Average) + 3σ (三西格玛):
*   EWMA是啥？ 一种加权平均，越近的数据权重越大，能平滑掉一些毛刺，反映近期趋势。
*   3σ是啥？ 统计学上，如果数据大致符合正态分布，99.7%的数据会落在均值加减3倍标准差的范围内。超出这个范围的点，就可以认为是异常。
*   怎么用？ 计算指标的EWMA值和标准差(σ)，然后用 EWMA + 3σ 作为上限，EWMA - 3σ 作为下限。
*   单尾型（只关心过高或过低）：就只用上界或下界。
*   STL 分解 (Seasonal and Trend decomposition using Loess) + residual 3σ:
*   STL是啥？ 能把时间序列数据分解成三部分：季节性（Seasonality）、趋势性（Trend）、剩余部分（Residual）。
*   怎么用？ 我们主要关心“剩余部分”。如果剩余部分的值超出了它的3σ范围，说明这个波动不是由季节性或趋势性引起的，可能是真异常。
*   优点： 能处理比较复杂的、非固定周期的季节性。

4.2 指标缺失或离散（数据质量一般或数据本身是事件计数）：
*   最新 N 点 IQR 法 (Interquartile Range):
*   IQR是啥？ 四分位距，等于P75减去P25。它比标准差更能抵抗极端异常值的影响。
*   怎么用？ 取最近的N个点（比如20个点），计算这N个点的P25和P75，然后 IQR = P75 - P25。异常范围通常定义为 [P25 - 1.5*IQR, P75 + 1.5*IQR] 之外。
*   优点： 对数据分布没啥要求，比较皮实。
*   针对离散事件计数用 Poisson CUSUM (泊松累积和控制图):
*   啥时候用？ 比如监控服务器错误日志出现的次数、安全攻击事件发生的次数这类“稀有事件计数”。这些事件平时很少，一旦发生就要注意。
*   怎么用？ 这个稍微复杂一点，它会累积观察值与期望值之间的差异，当累积差异超过一定阈值时报警。能更早地检测到发生率的微小但持续的变化。

“参数基线都靠最近 7d 数据滚动更新。”
重要！ 这些算法的参数（比如EWMA的平滑系数，3σ的σ值，IQR的P25/P75）不能一成不变，要定期（比如每天）用最近一周的数据重新计算和更新，这样算法才能适应指标自身的变化。

老鸟心得： 轻量级算法是静态规则的有力补充。EWMA+3σ是最常用的，也相对容易实现。但别忘了，它们引入了新的参数需要调整，也可能引入新的误报。

Step 5：仍有漏报/误报 → 使用“重算法”（Heavy Guns - The Last Resort）

如果轻量级算法还是搞不定，误报漏报依然很多，或者业务场景特别复杂（比如多个指标之间有复杂的关联关系），这时候才考虑上“重武器”——机器学习算法。

警告：这些算法需要更多的数据、更强的计算能力、更专业的知识来调参和维护。不要轻易使用！除非你真的需要，并且有能力驾驭它们。

5.1 季节性 + 长趋势：Facebook Prophet / Holt-Winters
*   Prophet: Facebook开源的时间序列预测库。对有明显多重季节性（日、周、年）和节假日效应的数据效果不错。相对易用。
*   Holt-Winters: 也是经典的时间序列模型，能处理趋势和季节性。

5.2 复杂噪音：ARIMA / SARIMA
*   这些是更传统但强大的统计模型，能处理更复杂的时间序列模式和自相关性。调参比较专业。

5.3 多维耦合（CPU + Load + MEM 等多个指标一起看）：IsolationForest、One-Class SVM、深度自编码 (Autoencoder)
*   场景： 有时候单个指标看起来都正常，但它们组合起来就暗示着问题。比如CPU不高，内存不高，但IO Wait很高，同时应用响应变慢，这可能说明磁盘I/O瓶颈。
*   Z-score 归一化： 不同指标的量纲和范围不一样，需要先归一化（比如都转换成均值为0，标准差为1的分布），才能放到一个模型里比较。
*   IsolationForest (孤立森林): 一种基于树的异常检测算法，异常点更容易被“孤立”出来。
*   One-Class SVM (单类支持向量机): 学习正常数据的边界，然后判断新数据是否在边界内。
*   深度自编码 (Deep Autoencoder): 一种神经网络。用正常数据训练它去“复制”输入。如果输入一个异常数据，它复制出来的效果就会很差（重构误差大），以此判断异常。
*   部署： 通常是每天离线训练模型（用过去N天的数据），然后把模型部署到线上做实时推理（判断新来的点是不是异常）。

老鸟心得： 到了这一步，你可能需要数据科学家或者算法工程师的帮助了。运维工程师的重点是理解这些算法的适用场景和局限性，以及如何将它们集成到监控告警流程中。95%以上的场景，靠Step 3和Step 4就能解决。

Step 6：告警策略与回调（Making Alerts Actionable and Not Annoying）

规则配好了，告警怎么发？发给谁？怎么避免告警风暴？这都是学问。

6.1 告警分级 (Alert Severity)
*   Warning (警告): 可能有问题，需要关注，但不紧急。比如磁盘空间60%。
*   Major/Average (一般/严重): 业务可能已受影响，或即将受影响，需要及时处理。比如CPU使用率80%持续5分钟。
*   Critical (紧急/致命): 业务已严重受影响或中断，需要立即响应！比如核心服务QPS为0。
*   怎么定级？ 参考Step 0的故障分级策略和对业务的实际影响。

6.2 抑制策略 (Alert Suppression/Inhibition/Silencing)
*   目标： 减少告警噪音，避免告警风暴，让运维聚焦在真正重要的问题上。
*   常见策略：
*   去抖动 (Debouncing/Flapping): 一个指标在阈值上下反复横跳，导致频繁告警和恢复。可以设置“连续N个点异常才告警”，“恢复后持续M分钟稳定才发送恢复通知”。
*   依赖抑制： 如果服务器都挂了（ping不通），那它上面的CPU、内存告警就没必要发了，只发服务器宕机告警。
*   临时静默 (Silencing): 计划内维护、发布变更时，可以临时屏蔽相关告警。
*   聚合 (Aggregation): 100台机器的CPU都高了，发1条聚合告警，而不是100条独立告警。
*   手册里的例子： "同主机同维度 30 min 内最多一次" – 这是时间窗口抑制。 "自动关闭需满足‘恢复阈值’ + ‘保持 10 min 稳定’" – 这是带稳定期的恢复。

6.3 回调/通知 (Notification & Escalation)
*   发给谁？
*   Warning：可能发到团队群聊（企业微信、Slack、钉钉），或者邮件列表。
*   Major/Critical：除了群聊，可能需要电话、短信通知值班人员 (PagerDuty, OpsGenie等)。
*   通知内容： 告警信息要清晰！包括：什么时间，哪个集群/主机/业务，哪个指标，当前值是多少，阈值是多少，告警级别，可能的排查方向或预案链接（Playbook URL）。
*   升级策略 (Escalation): 如果一个Critical告警发出后5分钟内无人认领(Acknowledge)，自动升级给二线或主管。
*   自动化动作 (Optional):
*   自动创建工单 (JIRA, ServiceNow)。
*   触发一些简单的自动化恢复脚本（比如重启进程，清理临时文件——这个要非常谨慎！）。

老鸟心得： 告警的“用户体验”非常重要。没人喜欢被无效告警淹没。抑制策略和清晰的告警信息是关键。

Step 7：效果评估 & 调参闭环（Review and Iterate - Constant Improvement）

没有完美的告警规则，只有持续优化的规则。

回测历史数据 (Backtesting):

拿过去一段时间（比如一个月）的历史数据和已知的故障事件。
用你新配的规则跑一遍，看看它能不能准确报出已知故障（召回率 Recall），同时又没有报出一堆不是故障的“狼来了”（精确率 Precision，误报率 False Positive Rate）。
F1-Score: 综合召回率和精确率的指标，越高越好。
定期 Review (比如每两周或每月)：

召回率 (Recall): 真正发生的故障，我们报出来了多少？（目标：越高越好）
精确率 (Precision) / 误报率 (False Positive Rate): 我们报的告警里，有多少是真家伙，多少是“狼来了”？（目标：精确率越高越好，误报率越低越好）
MTTA (Mean Time To Acknowledge): 从告警发生到有人响应平均花了多久？
MTTR (Mean Time To Resolution): 从故障发生到解决平均花了多久？
生产事故复盘 (Post-mortem): 每次出事后，都要回头看：我们的监控告警发现问题了吗？是第一时间发现的吗？规则有没有需要调整的？
规则更新：

业务逻辑变了、系统架构调整了、指标含义变了（比如换了个采集器，单位从MB变成GB了），都要重新审视甚至重走一遍Step 1-3。
根据Review结果，不断调整阈值、窗口大小、算法参数。
老鸟心得： 这是一个PDCA (Plan-Do-Check-Act) 的循环。配置规则只是开始，持续的评估和优化才是运维水平提升的关键。把每次误报和漏报都当成学习和改进的机会。

总结（Key Takeaways for Little Bro）

先懂业务，再谈监控： 不懂指标是干啥的，SLO是啥，配出来的告警就是添乱。
KISS原则永不过时： 先上最简单的静态阈值、中断告警。能解决80%的问题。
看图！看图！看图！ (Step 2 数据摸底) 花时间去了解你的指标曲线，这是定规则的基础。
小步快跑，持续迭代： 先把基础规则搞上线，然后根据实际运行情况（误报、漏报）不断优化。别想一口吃成胖子。

您不应该做的事情：

不要在回复中包含任何编程语言（Python）。 相反，您应该用自然语言（中文）提供一个有序的步骤列表，并给出具体的描述。
不要自行将时间戳转换为日期时间或将日期时间转换为时间戳。 这些详细过程将由执行者处理。
不要使用本地数据（在特定时间段内过滤/缓存的序列）来计算聚合的“组件-KPI”时间序列的全局阈值。 始终使用指标文件中特定组件的整个KPI序列（通常包括一天的KPI）来计算阈值。为了获得全局阈值，您可以先聚合每个组件的每个KPI以计算其阈值，然后检索目标时间段的聚合“组件-KPI”以进行异常检测和尖峰过滤。
不要通过Python可视化数据或绘制图片或图表。 执行者只能提供基于文本的结果。代码中绝不包含matplotlib或seaborn库。
不要在本地文件系统中保存任何内容。 将中间结果缓存到IPython Kernel中。代码单元中绝不使用bash命令。
不要在给定时间段内过滤数据后再计算阈值。 始终在过滤给定时间段数据之前，使用指标文件中特定组件的整个KPI序列计算全局阈值。
不要在不知道有哪些KPI可用时查询特定KPI。 不同系统可能有完全不同的KPI命名约定。如果您想查询特定KPI，请首先确保您了解所有可用的KPI。
不要错误地将追踪中包含故障组件的下游端健康（非故障）服务识别为根本原因。 根本原因组件应是追踪调用链中出现的最下游故障服务，必须首先是通过指标分析识别出的故障组件。
不要在日志分析时仅关注警告或错误日志。许多信息日志包含有关服务操作和服务之间交互的关键信息，这些信息对根本原因分析非常有价值。
不要在回复中使用英文。 使用中文。
"""